# Publish MiniBook3 v1.0.4

This sheet is the warehouse checklist. Eng has bumped `version.py` to **1.0.4** and written multi-OS notes. You build installers, upload heavy binaries by hand, then flip pointers.

**Do not** commit `*.exe`, `*.zip`, or large tarballs. **Do not** push `windows/latest/stable/latest.txt` (or Linux `latest.txt`) until every public asset returns **HTTP 200**.

**Git default branch must be `main`** (D127). POS Check now reads `raw.githubusercontent.com/…/MiniBook3-Downloads/main/…`. Pushing only to another branch leaves shops on HTTP 404.

Keep Admin “latest” / public Downloads on **v1.0.3** (Windows) / current Linux pointer until this tag is verified.

**Notes file (GitHub Release body):** [`RELEASE_NOTES_v1.0.4.md`](./RELEASE_NOTES_v1.0.4.md)  
**Layout rules:** [`../layout.md`](../layout.md)

### Staged locally (2026-08-24) — Linux from `Linux-MiniBook3/release`

| Location | Contents |
|----------|----------|
| `linux/v1.0.4/` | `install.sh`, `download-manifest.json`, slim `SHA256SUMS.txt` (commit-safe) |
| `github-release-assets/v1.0.4/` | Same small files + notes + `stable-latest.txt` for GitHub drag-upload |
| `staging/v1.0.4/` | Binaries (tar.gz / AppImage / `.deb`) — **gitignored**; upload to GitHub Release from here |

**Not flipped yet:** `linux/latest/stable/latest.txt` still **1.0.2** (wait for HTTP 200 on public assets).

---

## Order (safety rule)

1. Confirm production API has admin-password routes (`POST /api/minibook3/admin-password/request-token`) — required for this wave’s story.
2. Build on each OS host: `make sync-packaging-versions` → `make release-*` → verify checksums → sign-off.
3. Create GitHub Release tag **`v1.0.4`** and attach binaries + small assets below. Paste **RELEASE_NOTES_v1.0.4.md** as the Release body.
4. Confirm every public URL returns **HTTP 200**.
5. Then commit and push pointer files under `windows/` / `linux/` (that flips auto-update via `latest.txt`). **Do not move those directories.**
6. Then paste versioned URLs into Admin → Releases (slug `minibook3-v1-0-4`).
7. Only then update MiniBook3 website seed / fallback (`releases.ts` / `releases_data.py` / `init.sql`).

Existing shops read:

`https://raw.githubusercontent.com/ShashikaUdara/MiniBook3-Downloads/main/windows/latest/stable/latest.txt`

If that file says `1.0.4` before the zip is on GitHub, **Check for Updates** 404s. Upload first, push git second.

---

## GitHub “latest” flag

Prefer both Windows and Linux assets on the same tag before setting GitHub’s “latest release”.

Linux one-line install may use `/releases/latest/download/install.sh`. If v1.0.4 becomes GitHub “latest” without Linux files, that Linux bootstrap breaks. Prefer versioned URLs from the notes file during staged ships.

Windows auto-update does **not** need the GitHub latest flag. It uses the git `latest.txt` pointer, then the versioned zip URL.

When creating the v1.0.4 release in the GitHub UI: leave “Set as the latest release” unchecked until both platforms you intend to ship are attached.

---

## 1. Attach these files to GitHub Release `v1.0.4`

### Binaries (from each OS build — do not git-commit)

| File | Role |
|------|------|
| `minibook3-1.0.4-win64-setup.exe` | Recommended Windows shop install |
| `minibook3-1.0.4-win64-portable.zip` | Windows portable |
| `minibook3-1.0.4-linux-x86_64.tar.gz` | Linux main payload |
| `minibook3_1.0.4_amd64.deb` | Optional |
| `minibook3-1.0.4-linux-x86_64.AppImage` | Optional |

Fill SHA-256 after build (paste into Release assets sheet / `SHA256SUMS.txt`).

### Small files (drag onto the same Release)

| GitHub asset name | Local source (typical) |
|-------------------|------------------------|
| `SHA256SUMS.txt` | Combined checksums for **this** tag |
| `install.ps1` | Windows one-line installer (from POS `release/` or `windows/v1.0.4/`) |
| `install.sh` | Linux one-line installer (from POS `release/` or `linux/v1.0.4/`) |
| `RELEASE_NOTES_v1.0.4.md` | [`docs/releases/RELEASE_NOTES_v1.0.4.md`](./RELEASE_NOTES_v1.0.4.md) |
| `download-manifest.json` | When generated by publish-cdn targets |
| `stable-latest.txt` | Channel pointer content (`1.0.4`) — attach for warehouse only; **do not** push git `latest.txt` until 200 |

Release body: paste the full multi-OS notes file.

---

## 2. Public URLs to prove with HTTP 200

```
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/minibook3-1.0.4-win64-setup.exe
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/minibook3-1.0.4-win64-portable.zip
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/minibook3-1.0.4-linux-x86_64.tar.gz
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/SHA256SUMS.txt
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/install.ps1
https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/install.sh
```

```bash
curl -fsSI https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/minibook3-1.0.4-linux-x86_64.tar.gz
curl -fsSI https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/install.sh
```

```powershell
curl.exe -fsSI https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/minibook3-1.0.4-win64-setup.exe
```

One-line smoke (after 200):

```bash
curl -fsSL https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/install.sh | bash -s -- --dry-run
```

```powershell
irm https://github.com/ShashikaUdara/MiniBook3-Downloads/releases/download/v1.0.4/install.ps1 | iex -DryRun
```

---

## 3. After 200s — Admin + auto-update

1. Admin → Releases → create **MiniBook3 v1.0.4** / slug `minibook3-v1-0-4` / version `1.0.4`.
2. Paste **versioned** `/download/v1.0.4/…` URLs (never `/latest/download/…`).
3. Publish + mark latest only when smoke passes.
4. Push git `windows/latest/stable/latest.txt` (and Linux channel) with one line: `1.0.4` — paths stay under `windows/` / `linux/` as today.
5. Update SPA fallback / seed only after the above.

Rollback: point `latest.txt` back to the previous live version; keep old binaries ≥ 30 days.

---

## 4. Smoke (fresh VM)

- [ ] About shows **1.0.4** (Windows and Linux)
- [ ] Auth / licence host is `api-minibook3.zolestio.com` (not localhost)
- [ ] Admin password reset requests a token without localhost / generic 404
- [ ] `/downloads` shows v1.0.4 packages for both OS rows in a private window
- [ ] One-line install dry-run resolves v1.0.4 URLs

Canonical playbook: `MiniBook3/docs/release.md` · plan: `MiniBook3/docs/adhocs.md` **W53** / **D125**.
